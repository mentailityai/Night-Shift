# =============================================================================
# Night-Shift Preference Optimizer — Python Dependencies
# =============================================================================

# --- Web Framework (Capture Layer API) ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# --- Database ---
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.30.0          # Async PostgreSQL driver
pgvector>=0.3.0          # pgvector SQLAlchemy integration
alembic>=1.14.0          # Database migrations

# --- Task Queue ---
celery[redis]>=5.4.0     # Distributed task queue with Redis broker
redis>=5.0.0             # Redis client

# --- LLM Providers ---
openai>=1.50.0           # OpenAI + vLLM (OpenAI-compatible API)
anthropic>=0.40.0        # Anthropic Claude

# --- Embeddings ---
sentence-transformers>=3.0.0  # Local embedding models (BGE-M3)

# --- MCP Server ---
mcp>=1.0.0               # Model Context Protocol SDK

# --- Configuration ---
pydantic>=2.0.0
pydantic-settings>=2.0.0 # BaseSettings .env loading

# --- Logging ---
structlog>=24.0.0        # Structured JSON logging

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0            # Async test client for FastAPI
